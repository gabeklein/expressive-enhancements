{
  "name": "babel-plugin-transform-in-interable",
  "version": "1.1.0",
  "description": "Babel transform detects use of iterable with binary `in` expression and transform it to an indexOf check.",
  "main": "lib/index.js",
  "author": "Gabe Klein <gabe@termtm.com> (https://github.com/gabeklein)",
  "scripts": {
    "build": "babel ./src --out-dir ./lib",
    "dev": "babel ./src --out-dir ./lib --source-maps --watch",
    "prepare": "npm run build"
  },
  "license": "MIT",
  "keywords": [
    "iterate",
    "iterator",
    "babel",
    "expressive"
  ],
  "dependencies": {
    "babel-types": "^6.25.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1"
  }
}
